spring.application.name=inventory
# Update the port (3306 is default) and database name if needed
spring.datasource.url=jdbc:mysql://localhost:3306/inventory_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=your_password

# Automatically updates the database schema matching the entity classes
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
